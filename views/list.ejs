<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>市民の声一覧 | ○○市</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100">
  <div class="container mx-auto px-4 py-6">
    <header class="mb-6">
      <h1 class="text-2xl font-bold text-center text-blue-700">市民の声一覧</h1>
    </header>
    
    <div class="flex justify-end mb-4">
      <a href="/post" class="bg-blue-600 text-white px-4 py-2 rounded hover:bg-blue-700">
        新規投稿
      </a>
    </div>
    
    <div class="space-y-4">
      <% if (posts.length === 0) { %>
        <p class="text-center text-gray-500">投稿はまだありません</p>
      <% } else { %>
        <% posts.forEach(post => { %>
          <div class="bg-white p-4 rounded shadow">
            <div class="flex justify-between items-start mb-2">
              <span class="inline-block bg-blue-100 text-blue-800 px-2 py-1 rounded text-sm">
                <%= post.category %>
              </span>
              <span class="text-sm text-gray-500">
                <%= new Date(post.created_at).toLocaleString() %>
              </span>
            </div>
            <h2 class="text-lg font-semibold mb-2"><%= post.title %></h2>
            <p class="text-gray-700"><%= post.message %></p>
          </div>
        <% }); %>
      <% } %>
    </div>
  </div>
</body>
</html>